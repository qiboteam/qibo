name: docs

on:
  workflow_dispatch:
  push:
    branches: [main]
    tags:
      - "*"

jobs:
  version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.classify.outputs.version}}
    steps:
      - id: classify
        uses: qiboteam/workflows/actions/docs/version@uv

  deploy-docs:
    needs: [version]
    uses: qiboteam/workflows/.github/workflows/latest-stable.yml@25aeda1851ca8617a6933cdac199c95cc3c11796
    with:
      python-version: "3.11"
      trigger-label: "${{needs.version.outputs.version}}"
      project: qibo
      uv-extras: --no-dev --group docs --all-extras
