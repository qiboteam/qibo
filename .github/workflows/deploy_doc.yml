name: Deploy doc

on:
  push:
      branches: [master,test_workflow]

jobs:
  call-workflow-in-another-repo:
    uses: qibogang/workflows/.github/workflows/deploy-sphinx-docs-to-ghpages.yml@doc
    with:
      python-version: 3.9