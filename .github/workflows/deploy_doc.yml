name: Deploy docs

on: [workflow_dispatch] #TODO: remove this line and uncomment the following ones
#on: 
#  workflow_dispatch:
#  push:
#      branches: [master]
  


jobs:

  deploy-docs:
    uses: qibogang/workflows/.github/workflows/deploy-sphinx-docs-to-ghpages.yml@cache_pip
    with:
      python-version: 3.9